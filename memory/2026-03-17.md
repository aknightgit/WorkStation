# 2026-03-17 工作日志

## 协作模式（重要）
- Codex 在岗时：负责复杂 coding 的编排与分发给 MiniMax，并检查/验证最终代码输出。
- Codex 不在岗时：由 MiniMax 自行安排与开发。

## 项目进展
- 麻将项目优化：调整 game_screen 流程（人类需点“摸”、发牌按钮居中、动作按钮禁用态）、新增胡/碰/吃/杠的可行性检查接口、修复手牌末张重复显示（lastDrawnTile 不再额外追加）。
- 发现异常目录 `/{lib`（疑似错误创建），未删除。

## 变更与提醒
- git commit 失败：未配置 user.name / user.email，需要设置后再提交。

## 继续开发（已恢复）
- 清理 `game_screen.dart` 中未使用状态：移除 `_canAction` / `_showActionMenu` 与对应无效 setState 赋值，减少噪音状态。
- 调整 `remainingTiles` 计算：改为按可摸区间（排除尾部百搭保留区）显示剩余张数，牌墙数字更准确。
- `MahjongTileWidget` 点击行为改为受 `selectable` 控制，避免非可选状态误触。
- 新增稳定回合流转：AI 出牌后统一进入人类响应窗口（吃/碰/杠/胡）；若无可响应动作自动过牌进入下家；`过` 改为进入真实下家而非固定人类摸牌；吃/碰/杠后状态清理与下一步动作衔接完整，避免卡回合。

## 用户偏好（部署与存储）
- K哥要求：暂不解压 Flutter，由他自行选择目录。
- 重要约束：谨慎往极空间根目录写大文件，系统盘仅约 9.5G，避免撑满。

## 待办（飞书账号绑定）
- K哥准备给老婆配置飞书机器人并接入当前助手。
- K哥已发送一组 `cli_...` + 密钥样式字符串（疑似 Feishu App ID / App Secret），这不是用户 open_id。
- 已向 K哥 解释：`accounts` 是“多机器人应用账号”配置，不是“多用户ID”配置；单机器人场景下，用户可先发消息再走 pairing 生成绑定。
- 新情况：飞嫂侧是“完全新建的独立机器人（另一套 appId/appSecret）”。
- 已按双 bot 模式完成配置：`channels.feishu.accounts` 下接入 `kge` 与 `fisao` 两套账号，`defaultAccount` 设为 `kge`，`dmPolicy` 设为 `pairing`。
- 待飞嫂首次私聊机器人后进行 pairing approve，获取并记录其 `ou_...` open_id。

## HR向量化基础搭建
- 已完成 HR 基础设施：
  - 新增 SQLite 结构化库 `knowledge-base/hr/hr_core.db`（人才池/技能/招聘流程/知识文档/人才地图表）。
  - 新增本地向量化脚本 `knowledge-base/hr/scripts/hr_platform.py`（init/ingest/search）。
  - 新增 schema：`knowledge-base/hr/sql/talent_schema.sql`。
  - 新增并更新文档：`knowledge-base/hr/README.md`、`knowledge-base/hr/SETUP.md`。
- 已执行验证：
  - `hr_platform.py init` 成功。
  - `hr_platform.py ingest` 成功写入 `enterprise_kb`（8 chunks）。
  - `hr_platform.py search` 可返回语义检索结果。

## HR结构化数据重定向到MariaDB
- 已新增 MariaDB schema：`knowledge-base/hr/sql/talent_schema_mariadb.sql`。
- 已新增初始化脚本：`knowledge-base/hr/scripts/init_mariadb_hr.py`。
- 已增强 `hr_platform.py init`：支持 `--structured-backend mariadb|sqlite|both` 与 `HR_DB_*` 环境变量。
- 已在 MariaDB 创建 `HumanResource` 数据库并初始化表结构，验证表数量=10。

## 用户偏好（HR推进）
- K哥确认要做“飞书问答入口 + 人才检索指令”。
- K哥授权：后续由我主动引导 K嫂 上传文档到飞书目录，并收集飞书链接后入库。

## 交付物
- 已生成《OpenClaw_飞书全链路部署指南》Markdown+PDF，覆盖：极空间安装 OpenClaw、MiniMax M2.5 注册与配置、飞书 Bot 创建与接入、端到端验收与排障。

## 模型昵称与分工约定
- K哥新增昵称：`mini` -> MiniMax 模型；`coco` -> Codex 模型。
- 已在 OpenClaw 模型别名中添加：
  - `mini` -> `minimax-cn/MiniMax-M2.5`
  - `coco` -> `openai-codex/gpt-5.3-codex`
- 约定：用户若有指定模型，由对应模型“专人专干”。

## 股票数据状态检查（2026-03-17 19:36 CST）
- 已核查 MariaDB `openclaw`（192.168.3.241:33061）：
  - `stocks_tushare` = 0 行
  - `stock_daily_tushare` = 0 行
- 结论：今日股票数据未入库，历史数据也未完成刷新（当前为空）。
- 调度现状：`openclaw cron list` 为空；`crontab -l`（node）无任务。

## 股票链路修复（2026-03-17 晚）
- 已新增脚本：
  - `stock/refresh_stock_master_tushare.py`（优先 Tushare，受限时自动回退到 AkShare 代码名称表）
  - `stock/update_tushare_daily_all.py`（按 trade_date 一次性更新当日全市场）
  - `stock/backfill_tushare_history.py`（全量/增量历史回补）
  - `stock/run_daily_tushare_7pm.sh`（定时任务入口）
- 已执行：
  - 刷新 `stocks_tushare` 成功，当前 5489 行。
  - 当日 `20260317` 日线更新成功，写入/更新 5478 行（已再次补打，避免全量回补启动时的空窗）。
  - 历史回补已后台启动（全量，19900101 起，限速避免触发 Tushare 50次/分钟限制）。
- 已配置定时：
  - `CRON_TZ=Asia/Shanghai`
  - `0 19 * * * /home/node/.openclaw/workspace/stock/run_daily_tushare_7pm.sh`
